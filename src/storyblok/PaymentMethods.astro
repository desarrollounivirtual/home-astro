---
import { storyblokEditable } from '@storyblok/astro';
import PaymentMethodsComponent from '../components/PaymentMethods.astro';
const { blok } = Astro.props;

const title = blok.title || blok.Title || blok['Title'];
const subtitle = blok.subtitle || blok.Subtitle || blok['Subtitle'];
const link = blok.link || blok.Link || blok['Link'];
---
<div {...storyblokEditable(blok)}>
  <PaymentMethodsComponent 
    title={title} 
    subtitle={subtitle} 
    link={link} 
  />
</div>
